# PHASE 14 REPORT — דף המידע (ℹ)

**המגרש PRO** · ענף `claude/phase-8-identity-closure-zdfogy`

## Baseline commit

`d5200f3` (Phase 13, COMPLETE). 353 unit / 163 browser / 516 total, working tree clean.

## קומיטים

- `29302e1` — דף המידע עצמו.
- `b45e1ed` — תיקון רגרסיה בסרגל העליון שהכפתור החדש גרם לה, ונתפסה אחרי הקומיט הראשון.
- `fc20ea3` — תיעוד הרגרסיה בדוח הזה.
- הקומיט הנוכחי — `tests/e2e/topbar.e2e.js`, הבדיקה שסוגרת את הפער.

## מה נבנה

עזרה בתוך האפליקציה, שנפתחת **רק בלחיצה** — לא חלון שקופץ מעצמו.

**כפתור ℹ בסרגל העליון**, בין ☀ ל-⚙. זמין מכל מסך, ולכן התשובה תמיד במרחק לחיצה אחת מהמקום שבו נוצרה השאלה.

**דף המידע נפתח על הקטע של המסך שבו אתה עומד.** מי שלוחץ ℹ בתוך «ציונים» שואל על ציונים — לא על התפריט כולו. הקטע הרלוונטי נפתח, כל השאר נשארים מקופלים מתחתיו וזמינים לעיון. המיפוי נשען על `document.body.dataset.mod` הקיים, שכבר מתוחזק על ידי `go()` — בלי מצב חדש ובלי מעקב נפרד.

**13 קטעים:** מאיפה מתחילים · מבחני כושר · מערכי שיעור · כושר · משחקים · ביפ טסט · פוטו־פיניש · אלופי בית הספר · תלמידים וציונים · כלי כיתה · ידע · פינת תזונה · הנתונים שלך.

## מה במפורש לא נבנה

- **אין פתיחה אוטומטית.** לא בכניסה הראשונה ולא אחריה.
- **אין מפתח «כבר ראית»** ב-`localStorage`. זו לא רק החלטת מוצר אלא גם המימוש: בלי מפתח כזה, אין מנגנון שיכול לגרום לחלון להופיע מעצמו. בדיקה ייעודית מאמתת שלחיצה על ℹ אינה כותבת דבר לאחסון.
- ההדרכה הקיימת של הפוטו־פיניש (`pfGuideModal`, שכן קופצת בפעם הראשונה) לא נגעה — היא התנהגות קיימת ונפרדת.

## קבצים שהשתנו

| קובץ | מה |
|---|---|
| `index.html` | כפתור `#btnInfo` בסרגל העליון; חלון `#infoModal` עם 13 קטעי `<details>`, כל אחד עם `data-info="<mod>"`. |
| `hm-app.js` | `openInfo()` — בוחר את הקטע לפי המודול הפעיל, פותח אותו בלבד, וגולל אליו רק כשהוא נמוך מ-40% מגובה החלון (אחרת הכותרת וה-✕ נדחפים מחוץ למסך). |
| `hm-styles.css` | סגנון `.info-*`; כותרת דביקה ל-`#infoModal` בלבד כדי שה-✕ יישאר זמין גם בקטע עמוק; ובקומיט השני — הגנת הפריסה של הסרגל העליון (ראו «רגרסיה בסרגל העליון» למטה). |
| `hm-i18n.js` | `ui.info` — כיתוב הכפתור באנגלית, ערבית ורוסית. התוכן עצמו נשאר עברית, כמו כל תוכן העומק באפליקציה. |
| `tests/e2e/info14.e2e.js` | 8 בדיקות לדף המידע. |
| `tests/e2e/topbar.e2e.js` | 6 בדיקות לפריסת הסרגל העליון (ראו למטה). |
| `tests/e2e/run.js` | רישום החבילה. |
| `Hamegrash.html`, `sw.js` | פלט בנייה. |

לא נגעו: `hm-data.js`, זהות (`cid`), הרישום, הסכמה, ההסבות, `tools.att`, `stu.list`, `ft.roster`, ומודל `LessonSession`.

## בדיקות

| | יחידה | דפדפן | סה״כ |
|---|---|---|---|
| לפני | 353 | 163 | 516 |
| אחרי | **353** | **177** | **530** |
| כשלים / דילוגים | 0 / 0 | 0 / 0 | 0 / 0 |

שמונה הבדיקות של דף המידע מכסות: הכפתור קיים והחלון סגור בכניסה · גם במכשיר ריק לגמרי שום דבר לא קופץ · לחיצה פותחת · נפתח על הקטע הנכון לפי המסך · קטע אחד פתוח בלבד · מסך אחר מביא קטע אחר · מסך בלי קטע משלו נופל אחורה בלי לקרוס · סגירה עובדת והחלון לא חוזר · ולא נכתב דבר לאחסון.

## תיקונים שנתפסו בבדיקה חזותית

לפני הקומיט:

1. **הנקודות של הרשימה הופיעו בצד שמאל.** `inset-inline-end:0` בהקשר RTL נפתר לשמאל; הוחלף ל-`inset-inline-start`.
2. **הגלילה הבריחה את הכותרת וכפתור הסגירה.** `scrollIntoView({block:"start"})` גלל גם קטעים שכבר היו גלויים. הוחלף בגלילה מותנית, ונוספה כותרת דביקה.

## רגרסיה בסרגל העליון — נתפסה אחרי הקומיט (`b45e1ed`)

**מה קרה.** הכפתור השלישי בסרגל הוציא אותו מתקציב הרוחב בכל מסך שמציג גם כפתור «חזרה», ושם הכותרת נחת על השעון. הרגרסיה נכנסה ל-`29302e1` ולא נתפסה בו: כל 171 הבדיקות עברו, כי בדיקות `Playwright` מאמתות קיום, תוכן והתנהגות — לא חפיפה ויזואלית בין שני אלמנטים. היא התגלתה רק כשהתבקש צילום של מסך «מבחני כושר». מסך הבית היה תקין, ולכן הצילום הראשון לא חשף כלום — בבית אין כפתור «חזרה».

**מדידה** (חפיפה בפיקסלים בין `.brand .ttl` ל-`.sboard`, מסך מבחני כושר):

| רוחב | לפני | אחרי |
|---|---|---|
| 430px | חפיפה | ללא |
| 390px | 19.6 | ללא |
| 360px | 49.6 | ללא |

מסך הבית: ללא חפיפה בכל שלושת הרוחבים, והשעון נשאר בו.

**שורש הבעיה.** זו הייתה חפיפה ולא גלישה מפני ש-`.brand .ttl` הוא `white-space:nowrap`, וה-`div` שעוטף את השם ואת כותרת המשנה ישב על רצפת ה-`min-content` שלו — כלומר `.brand` לא יכול היה להצטמצם בפועל, גם עם `min-width:0` שכבר היה עליו. אימות האשמה נעשה בצילום של אותו מסך עם `#btnInfo` מוסתר: הסרגל היה תקין.

**התיקון, בשתי שכבות.** הגנה מבנית — `min-width:0` לעטיפה הפנימית, `text-overflow:ellipsis` לשם, ו-`flex:none` ל-`.sboard`, כך שבמקרה הקיצון השם מתקצר והפעולות לא נדחסות; חפיפה כבר אינה מצב אפשרי. החזרת מקום בפועל — כיווץ לוח המחוונים מתחת ל-430px, ומתחת ל-400px השעון נסוג, אך ורק במסכים שנושאים כפתור «חזרה» (`:has()`), כדי שמסך הבית ישמור עליו. השעון הוא הרכיב היחיד בסרגל שאינו פעולה, והמכשיר ממילא מציג שעה. בדפדפן בלי `:has()` ההגנה המבנית עדיין חוסמת חפיפה, במחיר קיצור קל של השם.

**הלקח לפיילוט.** סרגל עליון צפוף הוא מקום שבו תוספת של רכיב אחד שוברת פריסה, וחבילת בדיקות ירוקה אינה ראיה לכך שמסך נראה תקין.

## סגירת הפער — `tests/e2e/topbar.e2e.js`

הפער שתועד למעלה נסגר בבדיקה ייעודית: 6 בדיקות שמודדות את המלבנים בפועל ומחפשות חפיפה בכל צמד של «חזרה» × שם האפליקציה × לוח המחוונים, בשלושה רוחבי טלפון אמיתיים (430 · 390 · 360).

מכוסה גם: הסרגל אינו גולש מחוץ למסך; שלושת כפתורי הפעולה נשארים מעל 30×30 להקשה בכל רוחב; השעון נסוג רק במסך צר עם «חזרה» ונשאר במסך הבית; והשם מתקצר ב-`ellipsis` במקום לדרוך החוצה.

**הבדיקה אומתה מול הבאג עצמו.** התיקון בוטל זמנית, הבנייה חודשה, והבדיקות נצבעו אדום בדיוק במקום הנכון:

```
FAIL  מסך עם כפתור «חזרה» — ["ttl × sboard — 22.8px"]  @430
FAIL  מסך הבית                — ["ttl × sboard — 13.3px"]  @390
```

השורה השנייה היא ממצא חדש: בלי התיקון גם מסך הבית חפף ב-390px — מקרה שהמדידה הידנית לפני כן פספסה, מפני שנמדדה רק אחרי שהתיקון כבר היה בפנים. זו בדיוק הסיבה שבדיקה עדיפה על עין.

לאחר החזרת התיקון ה-`diff` של `hm-styles.css` חזר לריק, והחבילה ירוקה.

## גרסת המדריך המלא

קיימת גם גרסה חיצונית ארוכה יותר של אותו תוכן, כעמוד לשיתוף מחוץ לאפליקציה. דף המידע בתוך האפליקציה הוא הגרסה המקוצרת — מה שצריך בזמן אמת, במסך אחד.

## ורדיקט

**COMPLETE**
